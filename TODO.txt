U-net pros and applications

- check all transformations available and see that they dont affect the mask
- Fix Gradient Flow Plot (# epochs)
- Fix metrics
- FIX Corrupt JPEG data: premature end of data segment

WARNINGS: 
    Be careful when augmenting data (i.e. in mirror padding  avoid padding labeled regions to the edges )
    Don't do augmentation on the validation set

    Usually, the aspect ratio will not necessarily be preserved when resizing images. However, you should make sure that it isn’t severely distorted—for example, changing an image from a 2:1 ratio to 1:2 could be undesirable.


    Training:

        Resize your training images to a consistent shape suitable for the model’s input requirements.
        Validation and Testing:

        Take an image from the validation/test set and resize it to the model’s input dimensions.
        Run it through the model to get the predicted mask.
        Resize (inverse-transform) the predicted mask back to the original resolution of the ground-truth label.
        Compare the resized prediction to the ground truth to calculate your metrics.

gcloud compute scp /path/to/local/file.csv your-vm-name:~/ --zone=your-vm-zone --project=your-project-id
gcloud compute scp ./my_dataset.zip computervisionsegmentation:~/ --zone=us-central1-c --project=digital-well-455416-f7
gcloud compute scp ./my_dataset.zip computervisionsegmentation:/home/erikasalinas581/data/ --zone=us-central1-c --project=digital-well-455416-f7


MY COMMAND:
gcloud compute scp Dataset.zip computervisionsegmentation:/home/erikasalinas581/U-Net-Segmentation/ --zone=us-central1-c --project=digital-well-455416-f7


computervisionsegmentation
us-central1-c
digital-well-455416-f7